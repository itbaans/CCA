services:
  app:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////app/data/patients.db
    ports:
      - "8000:8000"
    volumes:
      - patient-data:/app/data
    restart: unless-stopped

volumes:
  patient-data:
